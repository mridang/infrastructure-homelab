{
  "name": "@mridang/k8",
  "scripts": {
    "format": "nx format --all",
    "format:check": "nx format:check --all",
    "lint": "nx run-many --target=lint --all --verbose",
    "lint:fix": "nx run-many --target=lint --all --verbose --fix",
    "test": "nx run-many --target=test --all --verbose",
    "build": "nx run-many --target=build --all --verbose",
    "deploy": "nx run-many --target=deploy --all --verbose",
    "deploy:dev": "nx run-many --target=deploy --all --verbose --yes --stack=dev --non-interactive"
  },
  "devDependencies": {
    "@mridang/eslint-defaults": "^1.0.0",
    "@nx/eslint-plugin": "^20.4.0",
    "@nx/jest": "^20.4.0",
    "@nx/linter": "^19.8.4",
    "@pulumi/eslint-plugin": "^0.2.0",
    "@pulumi/kubernetes": "^4.19.0",
    "@pulumi/pulumi": "^3.141.0",
    "@tsconfig/node20": "latest",
    "@types/jest": "^29.5.14",
    "@types/node": "^20.3.2",
    "dotenv": "^16.4.5",
    "eslint": "^8.57.1",
    "jest": "^29.7.0",
    "knip": "^5.43.6",
    "nx": "20.4.0",
    "prettier": "^3.1.1",
    "ts-jest": "^29.1.0",
    "ts-loader": "^9.5.1",
    "ts-node": "^10.9.2",
    "tslib": "^2.8.1",
    "typescript": "^5.1.3"
  },
  "private": true,
  "workspaces": [
    "apps/*"
  ]
}
